# 大屏音频素材

当前版本已接入 `public/audio` 下的正式 MP3，由大屏启动按钮解锁播放。

- `order-race-bgm.mp3`：循环背景音乐。
- `score-injection.mp3`：基础加分音效。
- `milestone.mp3`：阶段达成音效。
- `top-three.mp3`：进入前三音效。
- `champion.mp3`：全国第一音效。

庆祝开始时会压低 BGM；音效结束后再抬回。静音按钮同时控制 BGM 与音效。
