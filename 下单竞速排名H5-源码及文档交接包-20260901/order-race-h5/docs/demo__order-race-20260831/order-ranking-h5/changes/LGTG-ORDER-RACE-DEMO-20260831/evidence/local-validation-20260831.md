# 本地验证证据｜2026-08-31

- 任务：`LGTG-ORDER-RACE-DEMO-20260831`
- 环境：本地 Sites 开发服务与项目专属本地 D1
- 源码状态：候选 `ba098f5d9f9f7b64ad8d05adfb374937144fd4c2`
- 数据：全部为程序生成的演示姓名和演示订单号

## 1. 构建与静态检查

| 检查 | 结果 | 关键事实 |
| --- | --- | --- |
| `npm run db:generate` | 通过 | Drizzle 识别 `participants`、`orders` 两张表并生成 `drizzle/0000_square_mentallo.sql` |
| `npm run lint` | 通过 | 检查 `app`、`components/order-race`、`db`、`lib/order-race`，无产品代码错误 |
| `npm run build` | 通过 | Vinext 构建完成，包含 `/`、`/control` 与 4 个 API 路由 |
| `git diff --check` | 通过 | 无空白符错误 |

## 2. API 与数据库规则

### RUN-LOCAL-001A：全局订单并发去重

- 新增演示人员：`验收选手-982985`，返回 201。
- 标准化后同名再次新增：返回 409。
- 空订单号：返回 400；不存在的人员：返回 404。
- 同一订单号 `CODEX-DEMO-1788180982985-RACE` 一次使用大写、一次使用小写并发提交。
- 两次响应排序后为 `[201, 409]`；该人员最终订单数为 `1`。
- 结论：同一订单全场只写入一次，数据库唯一约束和错误提示均生效。

### RUN-LOCAL-001B：批量导入

- 输入两名新人员、其中一名重复一次，并带一个空姓名。
- 首次发现忽略数没有包含“同次输入内的重名”，记录为 `DEF-LOCAL-001`。
- 修复后重跑结果：`created=2`、`ignored=1`、`invalid=1`，通过。

## 3. 浏览器主流程

### RUN-LOCAL-002A：录入页与大屏联动

- 在 `/control` 新增 `浏览器验收-268667`，页面提示“人员已新增”，人员总数从 13 变为 14。
- 录入订单 `UI-DEMO-1788181268667`，页面提示“订单登记成功”，累计订单从 54 变为 55。
- 将同一订单号改为小写再次提交，页面提示“该订单已登记给 浏览器验收-268667”，累计订单保持 55。
- 打开大屏后 1.5 秒内可见该人员，行内容为“10 / 浏览器验收-268667 / 1 ORDERS”，累计订单显示 55。

### RUN-LOCAL-002B：横向尺寸

| 尺寸 | 榜单 | 页面高度 | 页脚 | 结论 |
| --- | --- | --- | --- | --- |
| 1920×1080 | 前 10 名完整 | `scrollHeight=1080` | 完整可见 | 通过 |
| 1366×768 | 前 10 名完整，最后一行底部约 702px | `scrollHeight=768` | 顶部约 722px、完整可见 | 通过 |

初版浏览器检查发现时钟首屏水合不一致和短横屏裁切，分别记录为 `DEF-LOCAL-002`、`DEF-LOCAL-003`；修复并重载后无错误弹层，新增、录单、重复拒绝、大屏同步全流程通过。

## 4. 当前结论

- 本地 L0/L1/L2：通过。
- 数据正确性：同名不重复、同订单全场不重复、失败请求不增加订单。
- UI：横向大屏、独立录入页、科技动效和两档横屏布局通过。
- 未覆盖：正式权限、误录改绑、活动历史和长时间高并发；按需求留到明日确认。
