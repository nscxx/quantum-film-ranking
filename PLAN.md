# 量子膜省份加权积分竞争榜开发计划

## 总体方案

保留原工程的 React/Vinext、Cloudflare D1、API、实时轮询、持久化和独立控制台；重做大屏视觉、后台录入与积分规则。

核心流程：

```text
选择省份 + 选择套餐 +1
                 ↓
根据套餐权重增加积分
                 ↓
更新省份排名、地图颜色、套餐积分与总积分
```

固定阈值着色比相对排名略简单，也更容易向客户解释。核心地图视觉工作量基本不变。

## 套餐积分规则

| 套餐 | 内容 | 单次积分 |
|---|---|---:|
| A套餐 | 5卷钻石70，送5卷PPF L领路者 | 130分 |
| B套餐 | 100卷PPF L领路者，送6卷PPF L领路者 | 170分 |
| C套餐 | 100卷PPF天穹系列，送6卷天穹Pro | 100分 |

- 省份积分等于该省所有有效套餐流水积分之和。
- 套餐总积分等于该套餐有效录入次数乘以权重。
- 大屏所有主要数值统一显示积分。
- 套餐占比按积分计算。
- 撤销记录时扣除对应套餐积分。
- 权重由服务端固定配置，客户端不能提交或修改积分。

## 大屏视觉

- 固定1920×1080设计画布，以1536×864参考图按1.25倍换算坐标。
- 其他分辨率只整体等比缩放，非16:9屏幕居中留边。
- 整体重写 [big-screen.tsx](/Users/xiaojun/Documents/量子膜排行榜大屏/下单竞速排名H5-源码及文档交接包-20260901/order-race-h5/components/order-race/big-screen.tsx)。
- 采用静态科技底板、透明3D素材、动态SVG地图和动态DOM文字四层结构。
- Logo与字体使用客户提供的正式源文件。
- 页面结构：
  - 标题“量子膜订单竞争榜”。
  - 当前维度“全国省份”。
  - 参与省份“31个”。
  - 顶部指标显示“总积分”。
  - 前三名显示省份和积分。
  - 左榜显示第4-16名，右榜显示第17-30名。
  - 第31名保留在地图和数据中，但不进入侧榜。
  - 底部标题改为“产品套餐积分”。
- 底部A、B、C套餐分别沿用青蓝、紫色、金色视觉，显示套餐内容、累计积分和积分占比。

### 地图热力规则

使用固定积分阈值：

| 积分 | 颜色 |
|---:|---|
| 0-999 | 青蓝色 |
| 1000-2999 | 蓝紫色 |
| 3000-4999 | 紫色 |
| 5000-6999 | 粉红色 |
| 7000及以上 | 金色 |

- 恢复右下角五档图例，单位改为“积分”。
- 图例、地图和服务端使用同一个`SCORE_COLOR_BANDS`配置。
- 省份加分只重新判断该省颜色，不受其他省份积分变化影响。
- 撤销导致积分跨越阈值时，省份颜色同步降级。
- 排名仍按省份总积分计算，与地图颜色分档相互独立。

## 数据结构与接口

重构 [db/order-race.ts](/Users/xiaojun/Documents/量子膜排行榜大屏/下单竞速排名H5-源码及文档交接包-20260901/order-race-h5/db/order-race.ts)。

`order_events`保存：

- `request_id`
- `province_code`
- `package_code`
- `points`
- `created_at`
- `revoked_at`

接口：

- `GET /api/ranking`
  - 返回31省积分、排名、颜色档位、套餐积分、占比、总积分、最近流水和更新时间。
- `POST /api/orders`
  - 接收`{ requestId, provinceCode, packageCode }`。
  - 服务端根据套餐代码写入130、170或100分。
  - 相同`requestId`重复提交只生效一次。
- `POST /api/orders/[id]/revoke`
  - 软撤销指定流水，重复撤销不重复扣分。
- `POST /api/control/login`
- `POST /api/control/logout`
- 删除人员新增、名单导入和订单号接口。

排名规则：

1. 有效积分降序。
2. 同积分时，先达到当前积分的省份优先。
3. 仍相同时按固定省份顺序排列。
4. 零积分省份仍参与排名。

使用新的空D1数据库，不迁移旧人名和演示订单，不写入演示种子。

## 管理后台

重做 [control-panel.tsx](/Users/xiaojun/Documents/量子膜排行榜大屏/下单竞速排名H5-源码及文档交接包-20260901/order-race-h5/components/order-race/control-panel.tsx)。

操作流程：

1. 选择省份。
2. 选择A、B或C套餐。
3. 显示套餐内容和本次增加积分。
4. 点击确认。
5. 返回“某省积分 +130、+170或+100”。
6. 大屏在1秒内更新。

后台提供：

- 总积分。
- 31省积分排名。
- 三种套餐累计积分。
- 最近录入记录。
- 最近有效记录单条撤销。
- 简单活动口令登录。

不提供清零、批量加分、权重编辑、套餐编辑、场次历史和订单号输入。

## 测试与验收

- A套餐一次增加130分，B套餐增加170分，C套餐增加100分。
- 同一省份A、B、C各录入一次后为400分。
- 撤销B套餐后回到230分。
- 省份积分总和、套餐积分总和和大屏总积分始终一致。
- 相同`requestId`并发提交只生效一次。
- 热力边界逐项验证：999、1000、2999、3000、4999、5000、6999、7000分。
- 验证加分和撤销跨越颜色阈值时地图正确变色。
- 1920×1080截图与参考图进行半透明叠加和差异热图校准。
- 1366×768保持等比缩放，超宽屏正确居中留边。
- 未登录用户不能调用新增或撤销接口。
- 后台录入成功后，大屏在1秒内更新且不发生布局跳动。

## 地图合规

地图使用自然资源部标准地图作为基础，保留行政边界和南海诸岛。公开展示前由客户确认审图号及适用审核流程。[标准地图服务系统](https://bzdt.tianditu.gov.cn/)，[《地图管理条例》](https://www.gov.cn/zhengce/zhengceku/2015-12/14/content_10403.htm)

## 已锁定规则

- 固定1920×1080画布。
- 固定大陆31个省级地区。
- 侧榜只显示前30名。
- A、B、C套餐及130:170:100权重固定。
- 所有主要数值显示积分。
- 套餐占比按积分计算。
- 地图采用五档固定积分阈值，并显示积分图例。
- 单场活动，不提供清零和历史场次。
- 允许撤销最近误录。
- 客户提供正式Logo、字体和品牌素材。
